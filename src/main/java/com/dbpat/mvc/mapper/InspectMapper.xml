<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dbpat.mvc.mapper.InspectMapper">
    <insert id="sequenceCacheN">
        SELECT
            SEQUENCE_NAME,
            CACHE_SIZE
        FROM ORA_SEQUENCES
        WHERE BIZ_AREA_ID = #{bizAreaId}
              AND TRGT_ID = #{trgtId}
              AND CACHE_SIZE > 400;
    </insert>
</mapper>